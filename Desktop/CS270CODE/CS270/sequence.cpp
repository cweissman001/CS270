/* In a file "sequence.cpp", write the implementations of the constructor and size function.  When you've done this successfully, "make" should compile and execute your test case from the previous step without error. 
*/

 sequence::sequence( ){
	 data[CAPACITY];
 }
    //Postcondition: The sequence has been initialized as an empty sequence.
 
	 
size_type size( ) const{
	size_type retsize;
	
	return retsize;
}
    //Postcondition: The return value is the number of items in the sequence.