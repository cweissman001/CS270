template<class> TBD> //not a template class
TBD minimum(TBD x, TBD y);
#include "min_impl.hpp"
/*implementation must be in header because the compiler needs it all to be together*/

//can also put the declaration in and implement file